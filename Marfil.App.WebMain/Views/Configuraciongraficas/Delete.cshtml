@using Marfil.Dom.Persistencia.Model
@using Marfil.Dom.Persistencia.Model.FicherosGenerales
@using Marfil.Dom.Persistencia.Model.Interfaces
@using Resources
@model Marfil.Dom.Persistencia.Model.Graficaslistados.ConfiguraciongraficasModel

@{
    ViewBag.Title = ((IToolbar)Model).Toolbar.Titulo;
}

<h2>@General.LblBorrar</h2>

<h3>@General.LblMensajeConfirmacionBorrado</h3>
<div>
    
    <hr />
    <dl class="dl-horizontal">

        <dt>
            @Html.DisplayNameFor(f=>Model.Titulo)
        </dt>

        <dd>
            @Model.Titulo
        </dd>
            
       
       
    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(f=>Model.Codigo)
        @Html.HiddenFor(f => Model.Idlistado)
        <div class="form-actions no-color">
            <button class="btn btn-info" type="Submit" ng-disabled="!MainFormIsEnable">
                <i class="ace-icon fa fa-trash bigger-110"></i>
                @General.LblContinuar
            </button>

            <a class="btn btn-info" href="@Url.Action("Index",new {id= Model.Idlistado })">
                <i class="ace-icon fa fa-undo bigger-110"></i>
                @General.LblCancelar
            </a>
            
        </div>
    }
</div>
